# Frontend Fixes - TODO List

## Issues to Fix:
1. Auth.css styling conflicts with dark theme from index.css
2. api.js has incorrect auth endpoints (missing /api prefix)
3. btn-primary class missing in Auth.css

## Progress:
- [x] Fix Auth.css - Update to use dark theme CSS variables
- [x] Fix api.js - Add /api prefix to auth endpoints
- [x] Test the fixes

